{
  "name": "Deep Live Cam (GPU)",
  "description": "GPU-accelerated real-time face swap using webcam, video, or images. Requires GFPGANv1.4 and inswapper_128_fp16.onnx models.",
  "url": "https://github.com/hacksider/Deep-Live-Cam.git",
  "install": [
    {
      "command": "git clone https://github.com/hacksider/Deep-Live-Cam.git"
    },
    {
      "command": "cd Deep-Live-Cam && python -m venv venv"
    },
    {
      "command": "cd Deep-Live-Cam && venv\\Scripts\\activate && pip install -r requirements.txt && pip uninstall -y onnxruntime onnxruntime-gpu && pip install onnxruntime-gpu==1.16.3",
      "platform": "windows"
    },
    {
      "command": "cd Deep-Live-Cam && source venv/bin/activate && pip install -r requirements.txt && pip uninstall -y onnxruntime onnxruntime-gpu && pip install onnxruntime-gpu==1.16.3",
      "platform": "linux"
    },
    {
      "command": "cd Deep-Live-Cam && source venv/bin/activate && pip install -r requirements.txt && pip uninstall -y onnxruntime onnxruntime-coreml && pip install onnxruntime-coreml==1.13.1",
      "platform": "macos"
    }
  ],
  "run": [
    {
      "name": "Launch Deep Live Cam (GPU - CUDA)",
      "command": "cd Deep-Live-Cam && venv\\Scripts\\activate && python run.py --execution-provider cuda",
      "platform": "windows",
      "type": "console"
    },
    {
      "name": "Launch Deep Live Cam (GPU - CUDA)",
      "command": "cd Deep-Live-Cam && source venv/bin/activate && python run.py --execution-provider cuda",
      "platform": "linux",
      "type": "console"
    },
    {
      "name": "Launch Deep Live Cam (CoreML - Apple Silicon)",
      "command": "cd Deep-Live-Cam && source venv/bin/activate && python3.10 run.py --execution-provider coreml",
      "platform": "macos",
      "type": "console"
    }
  ],
  "notes": [
    "🚨 Before running, manually place the following model files into the Deep-Live-Cam/models folder:",
    "- GFPGANv1.4",
    "- inswapper_128_fp16.onnx",
    "These are required for face-swapping to work.",
    "",
    "💡 Tip: Use a screen capture tool like OBS to stream your webcam output.",
    "📦 If you get errors with model loading, double-check the models are in the right folder and your Python version is 3.10."
  ]
}
